YAML:
  - data serialization language
  - easier to read
  - comments allowed
  - querying YAML relies on many different external rules
  - typically, preferred option for configuration

XML:
  - markup language
  - harder to read
  - comments allowed
  - well established option to query data in XML file
  - decreasing in popularity because JSON and YAML